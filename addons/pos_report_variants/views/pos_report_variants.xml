<?xml version="1.0" encoding="utf-8"?>
<odoo>
   <record id="view_pos_report_variants_wizard" model="ir.ui.view">
        <field name="name">pos.report.variants.wizard.form</field>
        <field name="model">pos.report.variants.wizard</field>
        <field name="arch" type="xml">
            <form string="Sales Details">
                <label string="Pos session" for="pos_session_id"/>
                <field name="pos_session_id" mode="tree" colspan="4"/>
                <footer>
                    <button name="generate_report" string="Print" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_report_pos_reports_variants" model="ir.actions.act_window">
        <field name="name">Session Report with Variants</field>
        <field name="res_model">pos.report.variants.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
